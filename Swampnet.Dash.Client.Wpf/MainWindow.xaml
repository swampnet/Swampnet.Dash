<Window x:Class="Swampnet.Dash.Client.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Swampnet.Dash.Client.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
	<ScrollViewer VerticalScrollBarVisibility="Auto">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>
			<DockPanel LastChildFill="True" Grid.Column="0" DataContext="{Binding Lefty}">
				<StackPanel Orientation="Vertical" DockPanel.Dock="Top">
					<Button Content="{Binding Name}"/>
				</StackPanel>
				<ItemsControl ItemsSource="{Binding Path=Messages}">

				</ItemsControl>
			</DockPanel>
			<DockPanel LastChildFill="True" Grid.Column="1" DataContext="{Binding Righty}">
				<StackPanel Orientation="Vertical" DockPanel.Dock="Top">
					<Button Content="{Binding Name}"/>
				</StackPanel>
				<ItemsControl ItemsSource="{Binding Path=Messages}">

				</ItemsControl>
			</DockPanel>
		</Grid>
	</ScrollViewer>
</Window>
